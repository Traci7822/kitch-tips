<!DOCTYPE html>
  <html>
    <head>
      <div class="greeting" align="center">
        <h1>Hello <%= @user.name %>!</h1>
        <hr width="75%">
      </div>
    </head>

    <body>
      <div class="create_tip" align="center">
        <h3><a href="/new">Add a new tip to Kitch-Tips!</a></h3>
        <hr width="75%">
      </div>

      <div class="user_tips" align="center">
        <div class="col-wrapper">
          <h2>Your Kitch-Tips: </h2>
          <% @user_tips.each do |tip| %>
            <div class="col-md-6"><h3><%= tip.name %></h3>
              <!-- change format to Month, Year -->
              <h5><%= tip.updated_at %></h5></div>
              <h4>- - -</h4>
          <% end %>
        </div>
        <hr width="75%">
      </div>

      <div class="logout" align="center">
        <h3><a href="/logout">Log Out</a> of your account</h3>
      </div>
    </body>
  </html>
